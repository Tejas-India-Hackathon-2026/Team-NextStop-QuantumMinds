<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Fraud Alerts</title>

    <style>
        body {
            background: #f3f4f6;
            font-family: Arial;
            padding: 30px;
        }

        .alert {
            background: white;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 6px solid #dc2626;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,.08);
        }

        .alert h3 {
            color: #dc2626;
        }

        .time {
            color: #6b7280;
            margin-top: 8px;
        }
    </style>
</head>

<body>

<h1>Fraud Alerts</h1>

<br>

<div id="alerts">

    <div class="alert">
        <h3>⚠ Suspicious Transaction</h3>
        <p>Transaction TXN10021 has a high fraud risk.</p>
        <p>Risk Score: <strong>89</strong></p>
        <div class="time">2 minutes ago</div>
    </div>

</div>

<script>

const socket = new WebSocket(
    "ws://localhost:8000/ws/USER001"
);

socket.onmessage = function(event) {

    const data = JSON.parse(event.data);

    const alertBox = document.createElement("div");

    alertBox.className = "alert";

    alertBox.innerHTML = `
        <h3>⚠ Real-Time Fraud Alert</h3>
        <p>${data.message}</p>
    `;

    document
        .getElementById("alerts")
        .prepend(alertBox);
};

</script>

</body>
</html>